{
  "name": "jca-climatizacao-crm",
  "version": "0.1.0",
  "private": true,
  "description": "CRM simples para captacao e gerenciamento de solicitacoes de orcamento da JCA Climatizacao.",
  "main": "backend/server.js",
  "scripts": {
    "dev": "node backend/server.js",
    "start": "node backend/server.js",
    "test": "node --test",
    "db:schema": "psql \"%DATABASE_URL%\" -f database/schema.sql",
    "db:seed": "psql \"%DATABASE_URL%\" -f database/seed.sql",
    "db:schema:unix": "psql \"$DATABASE_URL\" -f database/schema.sql",
    "db:seed:unix": "psql \"$DATABASE_URL\" -f database/seed.sql"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "connect-pg-simple": "^10.0.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "express-rate-limit": "^7.5.0",
    "express-session": "^1.18.1",
    "helmet": "^8.0.0",
    "pg": "^8.13.1"
  },
  "devDependencies": {}
}
